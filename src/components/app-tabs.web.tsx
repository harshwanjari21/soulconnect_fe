/**
 * app-tabs.web.tsx — stub
 *
 * The ConsultLive web layout uses the Stack + BottomNavigation
 * pattern from _layout.tsx. This Expo boilerplate file is unused.
 * Kept as a stub to avoid module resolution errors.
 */
export default function AppTabsWeb() {
  return null;
}
