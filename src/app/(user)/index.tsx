/**
 * index.tsx — Discovery tab entry point
 *
 * Routes to the ConsultLive DiscoveryScreen.
 * Keeping this thin ensures all screen logic lives in src/screens/.
 */
import { DiscoveryScreen } from '@/screens/User/Discovery/DiscoveryScreen';

export default DiscoveryScreen;
